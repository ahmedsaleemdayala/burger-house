# Copy to .env.local for local dev (optional — dev uses the Vite proxy).
# On Vercel, set this in Project Settings > Environment Variables instead.

VITE_API_URL=https://burger-house-px60.onrender.com/api
